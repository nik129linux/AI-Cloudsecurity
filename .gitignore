# Local .terraform directories
**/.terraform/*

# .tfstate files (CONTIENEN SECRETOS - NO SUBIR)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*

# Exclude all .tfvars files, which are likely to contain sensitive data
# such as password, private keys, and other secrets.
*.tfvars
*.tfvars.json

override.tf
override.tf.json
*_override.tf
*_override.tf.json

.terraformrc
terraform.rc
